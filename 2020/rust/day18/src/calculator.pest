number = { ASCII_DIGIT }
term = { number | "(" ~ expression ~ ")"}
operator = {"*" | "+" | "-"}
expression = { term ~ (" " ~ operator ~ " " ~ term)+}
calculator = { (expression ~ "\n")+ }
file = {SOI ~ calculator ~ EOI}
